# Core engine runs on the standard library + numpy only.
numpy>=1.26

# Optional: only needed for the data downloaders in tools/
websockets>=12.0   # tools/fetch_derivs.py (derivatives history)
requests>=2.31     # tools/fetch_crypto.py (crypto candles)

# Telegram QR login (python serve.py on this PC)
telethon>=1.36
qrcode>=7.4
pillow>=10.0

# Passkey (WebAuthn) accounts on the desk
webauthn>=2.2
